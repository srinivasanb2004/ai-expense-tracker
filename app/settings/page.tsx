"use client"
import AppShell from "@/components/app-shell";import MobileNotifications from "@/components/mobile-notifications";import Toast,{ToastState} from "@/components/toast";import {AlertTriangle,Database,Download,Moon,ShieldCheck,Sun,Trash2,UserRound,X} from "lucide-react";import {useEffect,useState} from "react"
type Profile={name:string|null;email:string;createdAt:string}
export default function Settings(){const[theme,setTheme]=useState<"dark"|"light">("dark"),[confirm,setConfirm]=useState(false),[busy,setBusy]=useState(false),[profile,setProfile]=useState<Profile|null>(null),[name,setName]=useState(""),[toast,setToast]=useState<ToastState>(null);function say(message:string,type:"success"|"error"="success"){setToast({message,type});setTimeout(()=>setToast(null),2600)}useEffect(()=>{const saved=(localStorage.getItem('theme') as 'dark'|'light')||'dark';setTheme(saved);document.documentElement.classList.remove('dark','light');document.documentElement.classList.add(saved);fetch('/api/profile').then(r=>r.json()).then(d=>{if(d?.email){setProfile(d);setName(d.name||'')}})},[]);function toggle(){const n=theme==='dark'?'light':'dark';setTheme(n);localStorage.setItem('theme',n);document.documentElement.classList.remove('dark','light');document.documentElement.classList.add(n)}async function saveProfile(e:React.FormEvent){e.preventDefault();setBusy(true);const r=await fetch('/api/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({name})});setBusy(false);const d=await r.json().catch(()=>({}));if(!r.ok)return say(d.error||'Could not update profile','error');setProfile(d);say('Display name updated')}async function clear(){setBusy(true);const r=await fetch('/api/settings/clear-data',{method:'DELETE'});setBusy(false);if(!r.ok)return say('Could not clear data','error');setConfirm(false);say('All financial data cleared')}
const initial=(profile?.name||profile?.email||'U').charAt(0).toUpperCase();return <AppShell><Toast toast={toast} onClose={()=>setToast(null)}/><p className="eyebrow">Workspace</p><h2 className="mt-2 text-3xl font-black">Settings</h2><p className="mt-2 muted">Profile, privacy, appearance and data controls.</p><div className="mt-6 grid gap-4 lg:grid-cols-2"><div className="soft-panel lg:col-span-2"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><div className="grid h-16 w-16 shrink-0 place-items-center rounded-2xl bg-emerald-300 text-2xl font-black text-emerald-950">{initial}</div><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><UserRound className="accent" size={18}/><h3 className="font-black">Your profile</h3></div><p className="mt-1 truncate text-sm muted">{profile?.email||'Loading profile...'}</p>{profile&&<p className="mt-1 text-xs muted">Account created {new Date(profile.createdAt).toLocaleDateString('en-IN',{day:'numeric',month:'long',year:'numeric'})}</p>}</div></div><form onSubmit={saveProfile} className="mt-5 flex flex-col gap-3 sm:flex-row"><input value={name} onChange={e=>setName(e.target.value)} className="input" placeholder="Display name" required/><button disabled={busy} className="btn btn-primary sm:w-48">Save name</button></form></div><div className="soft-panel"><ShieldCheck className="accent"/><h3 className="mt-4 font-black">Privacy</h3><p className="mt-2 text-sm leading-6 muted">Your financial records are scoped to your authenticated user account.</p></div><div className="soft-panel"><Database className="accent"/><h3 className="mt-4 font-black">Export your data</h3><p className="mt-2 text-sm muted">Download expense transactions as CSV.</p><a href="/api/export" className="btn btn-secondary mt-4"><Download size={17}/>Download CSV</a></div><div className="soft-panel lg:col-span-2"><div className="flex items-center justify-between gap-4"><div>{theme==='dark'?<Moon className="accent"/>:<Sun className="accent"/>}<h3 className="mt-4 font-black">Appearance</h3><p className="mt-2 text-sm muted">Switch between dark and light mode.</p></div><button onClick={toggle} className={`relative h-8 w-16 rounded-full p-1 ${theme==='light'?'bg-emerald-300':'bg-white/10'}`}><span className={`block h-6 w-6 rounded-full bg-emerald-950 transition-transform ${theme==='light'?'translate-x-8':''}`}/></button></div></div></div><MobileNotifications/><div className="danger-panel mt-8"><div className="flex gap-4"><Trash2 className="text-red-400"/><div><h3 className="font-black text-red-300">Danger Zone</h3><p className="mt-2 text-sm muted">Delete all financial records while keeping your login account.</p><button onClick={()=>setConfirm(true)} className="mt-4 rounded-xl border border-red-500/30 bg-red-500/10 px-4 py-2 text-sm font-bold text-red-300">Clear all data</button></div></div></div>{confirm&&<div className="fixed inset-0 z-[100] grid place-items-center bg-black/70 p-4"><div className="modal-panel w-full max-w-md rounded-3xl border p-6"><div className="flex justify-between"><AlertTriangle className="text-red-400"/><button onClick={()=>setConfirm(false)}><X/></button></div><h3 className="mt-5 text-xl font-black">Clear all data?</h3><p className="mt-3 text-sm muted">This permanently deletes your financial data and cannot be undone.</p><div className="mt-6 flex justify-end gap-3"><button onClick={()=>setConfirm(false)} className="btn btn-secondary">Cancel</button><button onClick={clear} disabled={busy} className="rounded-xl bg-red-500 px-4 py-2 font-bold text-white">{busy?'Clearing...':'Clear everything'}</button></div></div></div>}</AppShell>}
