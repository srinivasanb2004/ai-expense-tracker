"use client";import {useState} from "react";import {signIn} from "next-auth/react";import {useRouter} from "next/navigation";import Logo from "@/components/logo";
    export default function Login(){const r=useRouter();const [msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const res=await signIn("credentials",{email:f.get("email"),password:f.get("password"),redirect:false});if(res?.error)setMsg("Invalid email or password");else r.push("/dashboard")}return <main className="grid min-h-screen place-items-center p-4"><form onSubmit={submit} className="glass w-full max-w-md rounded-3xl p-7"><Logo/><h1 className="mt-8 text-3xl font-black">Welcome back</h1><p className="mt-2 muted">Sign in to continue.</p><div className="mt-6 space-y-4"><input name="email" className="input" type="email" placeholder="Email" required/><input name="password" className="input" type="password" placeholder="Password" required/>{msg&&<p className="text-sm text-rose-400">{msg}</p>}<button className="btn btn-primary w-full">Sign in</button></div></form></main>}