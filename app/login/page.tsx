"use client"
import {useState} from "react"
import {signIn} from "next-auth/react"
import Logo from "@/components/logo"
export default function Login(){const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(busy)return;setBusy(true);setMsg("");const f=new FormData(e.currentTarget);try{const res=await signIn("credentials",{email:f.get("email"),password:f.get("password"),redirect:false});if(res?.error){setMsg("Invalid email or password");setBusy(false);return}window.location.assign("/dashboard")}catch{setMsg("Could not sign in. Check your connection and try again.");setBusy(false)}}return <main className="grid min-h-screen place-items-center p-4"><form onSubmit={submit} className="glass w-full max-w-md rounded-3xl p-7"><Logo/><h1 className="mt-8 text-3xl font-black">Welcome back</h1><p className="mt-2 muted">Sign in to continue.</p><div className="mt-6 space-y-4"><input name="email" className="input" type="email" placeholder="Email" required disabled={busy}/><input name="password" className="input" type="password" placeholder="Password" required disabled={busy}/>{msg&&<p className="text-sm text-rose-400">{msg}</p>}<button disabled={busy} className="btn btn-primary w-full">{busy?<><span className="auth-spinner"/>Signing in...</>:"Sign in"}</button></div></form></main>}
