"use client"
import {useState} from "react"
import Logo from "@/components/logo"
export default function Register(){const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(busy)return;setBusy(true);setMsg("");const f=new FormData(e.currentTarget);try{const res=await fetch("/api/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const d=await res.json().catch(()=>({}));if(!res.ok){setMsg(d.error||"Could not create account");setBusy(false);return}window.location.assign("/login")}catch{setMsg("Could not create account. Check your connection and try again.");setBusy(false)}}return <main className="grid min-h-screen place-items-center p-4"><form onSubmit={submit} className="glass w-full max-w-md rounded-3xl p-7"><Logo/><h1 className="mt-8 text-3xl font-black">Create your account</h1><div className="mt-6 space-y-4"><input name="name" className="input" placeholder="Full name" required disabled={busy}/><input name="email" className="input" type="email" placeholder="Email" required disabled={busy}/><input name="password" className="input" type="password" placeholder="Password (6+ characters)" required minLength={6} disabled={busy}/>{msg&&<p className="text-sm text-rose-400">{msg}</p>}<button disabled={busy} className="btn btn-primary w-full">{busy?<><span className="auth-spinner"/>Creating account...</>:"Create account"}</button></div></form></main>}
