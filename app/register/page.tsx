"use client";import {useState} from "react";import {useRouter} from "next/navigation";import Logo from "@/components/logo";export default function Register(){const r=useRouter();const [msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const res=await fetch("/api/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const d=await res.json();if(!res.ok)setMsg(d.error);else r.push("/login")}return <main className="grid min-h-screen place-items-center p-4"><form onSubmit={submit} className="glass w-full max-w-md rounded-3xl p-7"><Logo/><h1 className="mt-8 text-3xl font-black">Create your account</h1><div className="mt-6 space-y-4"><input name="name" className="input" placeholder="Full name" required/><input name="email" className="input" type="email" placeholder="Email" required/><input name="password" className="input" type="password" placeholder="Password (6+ characters)" required minLength={6}/>{msg&&<p className="text-sm text-rose-400">{msg}</p>}<button className="btn btn-primary w-full">Create account</button></div></form></main>}