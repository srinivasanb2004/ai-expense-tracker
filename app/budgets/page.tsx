"use client"
import AppShell from "@/components/app-shell"
import { AlertTriangle, Target } from "lucide-react"
import { useEffect, useState } from "react"
const categories=["Food","Transport","Shopping","Bills","Health","Entertainment","Education","Other"]
export default function Budgets(){const[items,setItems]=useState<any[]>([]);const[busy,setBusy]=useState(false);const[msg,setMsg]=useState("");async function load(){const r=await fetch("/api/budgets");if(r.ok)setItems(await r.json())}useEffect(()=>{load()},[]);async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const f=new FormData(form);setBusy(true);const r=await fetch("/api/budgets",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json().catch(()=>({}));setBusy(false);if(!r.ok){setMsg(d.error||"Could not save budget");return}form.reset();setMsg("Budget saved");load()}return <AppShell><h2 className="text-3xl font-black">Budgets</h2><p className="muted">Set category limits for the current month.</p><form onSubmit={save} className="soft-panel mt-6 flex flex-col gap-3 sm:flex-row"><select name="category" className="input">{categories.map(x=><option key={x}>{x}</option>)}</select><input name="amount" className="input" type="number" min="1" step=".01" placeholder="Monthly limit ₹" required/><button disabled={busy} className="btn btn-primary sm:w-48">{busy?"Saving...":"Set budget"}</button></form>{msg&&<p className="mt-3 text-sm muted">{msg}</p>}<div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.map(b=>{const p=b.amount?Math.min(100,b.spent/b.amount*100):0;return <div key={b.id} className="stat-card"><div className="flex justify-between"><div><b>{b.category}</b><p className="text-sm muted">₹{b.spent.toLocaleString("en-IN")} of ₹{b.amount.toLocaleString("en-IN")}</p></div>{b.spent>=b.amount?<AlertTriangle className="text-amber-300"/>:<Target className="accent"/>}</div><div className="progress-track mt-5"><div className="progress-value" style={{width:`${p}%`}}/></div><p className="mt-2 text-right text-xs muted">{p.toFixed(0)}%</p></div>})}</div></AppShell>}
