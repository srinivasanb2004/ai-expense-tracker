"use client"
import AppShell from "@/components/app-shell"
import { useEffect,useState } from "react"
export default function Income(){const[xs,setXs]=useState<any[]>([]);async function load(){const r=await fetch("/api/incomes");if(r.ok)setXs(await r.json())}useEffect(()=>{load()},[]);async function add(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const f=new FormData(form);const r=await fetch("/api/incomes",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});if(r.ok){form.reset();load()}}return <AppShell><h2 className="text-3xl font-black">Income</h2><p className="muted">Track salary, freelance and other money coming in.</p><form onSubmit={add} className="soft-panel mt-6 grid gap-3 md:grid-cols-2"><input name="source" className="input" placeholder="Salary / Freelance / Other" required/><input name="amount" className="input" type="number" min="1" step=".01" placeholder="Amount" required/><input name="date" className="input" type="date" required/><select name="category" className="input"><option>Salary</option><option>Freelance</option><option>Business</option><option>Other</option></select><button className="btn btn-primary md:col-span-2">Add income</button></form><div className="soft-panel mt-6">{xs.length?xs.map(x=><div key={x.id} className="flex justify-between border-t border-white/8 py-4"><span className="font-bold">{x.source}</span><span className="accent font-black">+₹{x.amount.toLocaleString("en-IN")}</span></div>):<p className="py-8 text-center muted">No income added yet.</p>}</div></AppShell>}
